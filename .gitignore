node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.local
*.log

# pnpm leftovers — this repo uses npm workspaces, not pnpm
pnpm-lock.yaml

# Stray per-workspace lockfiles — npm workspaces consolidates into the root lockfile
ui/package-lock.json

# Publish artifacts
*.tgz

# Playwright MCP session artifacts (page snapshots + console logs)
.playwright-mcp/
